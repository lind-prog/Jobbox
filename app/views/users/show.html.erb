<div class="container">
  <div class="container-dashboard">
    <div class="dashboard-container">
      <div class="dashboard-header">
        <% if current_user.recruiter? %>
          <%= render "recruiter", user: @user %>
        <% else %>
          <%= render "jobseeker", user: @user %>
        <% end %>
      </div>

      <%= render "calendrier" %>
    </div>
  </div>
</div>
